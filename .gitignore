# Secrets — never commit
.env
.env.*
!.env.example

# Runtime data (credential store with refresh tokens) — never commit/bake
data/

# Python
**/__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
**/.pytest_cache/
